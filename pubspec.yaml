name: skill_sharing_platform
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.2.2
  video_player: ^2.8.2
  connectivity_plus: ^3.0.3
  flutter_secure_storage: ^8.0.0
  shared_preferences: ^2.2.2
  provider: ^6.0.5
  image_picker: ^1.0.4
  path_provider: ^2.1.2
  path: ^1.9.0
  video_compress: ^3.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon/app.ico"

flutter:
  uses-material-design: true

  assets:
    - "assets/images/"
    - "assets/videos/"
